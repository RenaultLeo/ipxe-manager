# Bandit — analyse statique Python (règles OWASP → identifiants CWE).
# CI : severity medium+ (-ll) bloque la merge ; SARIF (JSON→scripts/bandit_json_to_sarif.py) → Code scanning.
exclude_dirs:
  - tests
  - /venv
  - /.venv
