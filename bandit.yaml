# Bandit — analyse statique Python (règles OWASP → identifiants CWE).
# CI : severity medium+ (-ll) bloque la merge ; SARIF → Security / Code scanning (catégorie bandit).
exclude_dirs:
  - tests
  - /venv
  - /.venv
