# Bandit — analyse statique Python (règles OWASP → identifiants CWE).
# CI : `-ll` = échec uniquement si sévérité MEDIUM ou HIGH (LOW → rapport complet seulement).
exclude_dirs:
  - tests
  - /venv
  - /.venv
