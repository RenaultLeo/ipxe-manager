#!ipxe

########################################
# Sous-menu {{ os_type.label }} — Scripts personnalisés
########################################

:start
{% include '_theme.inc.j2' %}
menu === {{ os_type.label }} — Scripts personnalisés ===
{% for e in entries %}
item c{{ e.id }} {{ e.label }}
{% endfor %}
item --gap
item back {{ back_item_label }}
choose selected || goto back
goto ${selected}

{% for e in entries %}
:c{{ e.id }}
  chain --autofree {{ e.custom_ipxe }}
{% endfor %}

:back
chain --autofree {{ back_menu_url }}
